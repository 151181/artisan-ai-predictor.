# MongoDB connection string
MONGODB_URL="mongodb+srv://BillyGakungi:TniuEZZbg5xM36Tb@cluster0.mwk5fk5.mongodb.net/?appName=Cluster0"

# This variable is used by your database.js file to specify the database name
MONGO_DB_NAME="artisanDB" 
# You can change "artisanDB" to any database name you want to use.

# Other variables
PORT=5000
JWT_SECRET=12345678910   
# Replace with a strong secret key for JWT authentication
FRONTEND_URL="http://localhost:3000"
# URL of the frontend application for CORS configuration
SESSION_SECRET="your_session_secret_key"
# Replace with a strong secret key for session management
NODE_ENV="development"
# Set to "production" in a production environment
BASE_API_URL="/api"
# Base URL for API endpoints
LOG_LEVEL="info"
# Logging level (e.g., info, debug, error)
SESSION_LIFETIME=86400000
# Session lifetime in milliseconds (e.g., 86400000 ms = 1 day)  
# Add any additional environment variables as needed
DEBUG_MODE=true
# Set to true to enable debug mode  
# Add any additional environment variables as needed
ENABLE_CACHE=false
# Set to true to enable caching mechanisms  
CACHE_TTL=600
# Cache time-to-live in seconds 
MAX_UPLOAD_SIZE=10485760
# Maximum upload size in bytes (e.g., 10485760 bytes = 10 MB
ALLOWED_FILE_TYPES="image/jpeg,image/png,application/pdf"
# Comma-separated list of allowed file MIME types for uploads   
RATE_LIMIT_WINDOW=15
# Rate limiting window in minutes   
RATE_LIMIT_MAX=100
# Maximum number of requests per window per IP address
EMAIL_SERVICE="Gmail"
# Email service provider for sending emails 
EMAIL_USER="                
# Email address for sending emails
EMAIL_PASSWORD="your_email_password"    
# Password for the email account
# Add any additional environment variables as needed
SMTP_HOST="smtp.gmail.com"
# SMTP host for email service       
SMTP_PORT=587
# SMTP port for email service   
USE_TLS=true
# Set to true to use TLS for email service  
DEFAULT_LANGUAGE="en"
# Default language for the application  
TIMEZONE="UTC"  
# Default timezone for the application
ENABLE_2FA=false
